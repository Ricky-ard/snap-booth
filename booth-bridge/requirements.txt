fastapi==0.110.1
uvicorn==0.25.0
httpx>=0.27.0
pydantic>=2.6.4

# Camera drivers (install the one for your OS)
# macOS + Linux:
#   brew install libgphoto2   # or  apt install libgphoto2-dev
#   pip install gphoto2
#
# Windows (digiCamControl):
#   install digiCamControl from https://digicamcontrol.com and enable the Web Server
#
# Webcam fallback (always safe to install):
opencv-python>=4.9.0

# Printer drivers (auto-detected):
#   macOS + Linux use `lp` (built-in) — no extra install
#   Windows requires pywin32:
#     pip install pywin32
